<%#= form_with url: chats_path, method: :post, local: false, id: "chat-form", data: { turbo_stream: true }, class: "flex items-center p-4 border-t" do %>
  <%#= text_field_tag :content, nil, placeholder: "Type a message...", class: "flex-grow p-2 border rounded-l-md" %>
  <%#= submit_tag "Send" %>
<%# end %>

<!--<div id="chat-container" class="overflow-y-auto p-4 space-y-4">-->
  <%#= turbo_stream_from "chats" %>
  <%#= render @chats %>
<!--</div>-->


<div class="flex h-screen w-full">
  <!-- Include Sidebar Partial -->
  <%= render 'sidebar' %>

  <!-- Right Panel: Chat Messages -->
  <div class="w-full md:w-3/4 p-4 flex flex-col">
    <%= render 'chat' %>
  </div>
</div>
